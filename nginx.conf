# 🚨💡 Viewers pls replace my domain with your domain here

server {
    listen 80;
    server_name ${VITE_DOMAIN_NAME};
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl;
    server_name ${VITE_DOMAIN_NAME};

    ssl_certificate /etc/letsencrypt/live/fs.atulpro.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/fs.atulpro.com/privkey.pem;

    # Strong SSL Settings
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers EECDH+AESGCM:EDH+AESGCM:AES256+EECDH:AES256+EDH;
    ssl_ecdh_curve secp384r1;
    ssl_session_timeout  10m;
    ssl_session_cache shared:SSL:10m;
    ssl_session_tickets off;
    ssl_stapling on;
    ssl_stapling_verify on;

    # Security Headers
    add_header Strict-Transport-Security "max-age=63072000; includeSubdomains";
    add_header X-Frame-Options DENY;
    add_header X-Content-Type-Options nosniff;

    # Increase client body size limit to 260MB
    client_max_body_size 260M;

    location / {
        proxy_pass http://localhost:${PORT};
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Install this for the port replacement with the one in the env you set
# sudo yum install -y gettext
# check the envsubst installation
# which envsubst 
# Replace the port in the nginx config
# envsubst '$PORT,$VITE_DOMAIN_NAME' < nginx.conf | sudo tee /etc/nginx/conf.d/default.conf
